# EXEC-002：次链路页面深度分析

> 执行智能体：补充链路执行智能体  
> 执行时间：2026-05-11  
> 来源：总控主智能体任务派发  
> 关联文件：
> - `STRUCT-001-结构完整性盘点.md`（结构规范产出，缺口已知）
> - `campus-miniapp-prototype.html`（完整原型，1425 行）
> - `campus_ui_decisions.md`（UI/交互/视觉决策）
> - `campus_rules.md`（产品硬规则）
> - `campus_glossary.md`（术语表）

---

## 分析范围总览

| # | 页面/维度 | screen ID | HTML 行号范围 | 类型 |
|---|----------|-----------|-------------|------|
| 1 | 社区 | `community` | L1168–1192 | 次链路页面 |
| 2 | 消息 | `message` | L1194–1205 | 次链路页面 |
| 3 | 我的 | `profile` | L1207–1348 | 次链路页面 |
| 4 | 二手详情 | `goods-detail` | L1087–1103 | 次链路页面 |
| 5 | 失物详情 | `lost-detail` | L1105–1120 | 次链路页面 |
| 6 | 认领申请 | `lost-claim` | L1122–1136 | 次链路页面 |
| 7 | 公告详情 | `announcement-detail` | L1350–1360 | 次链路页面 |
| 8 | 全局分析 | — | 全文件 | 导航/路由/搜索/CSS/响应式 |

> 图例：🔴 致命（阻断流程/违反硬规则） ｜ 🟡 严重（影响可用性/完整性） ｜ 🟢 建议（优化方向）

---

## 一、community（社区）— HTML L1168–1192

### 1.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1169–1173 | page-top：hidden back-btn + 标题 + "+"按钮（→publish-hub） |
| 快捷入口网格 | L1174–1179 | quick-grid 4 项：聊天官方群 / 交友官方群 / 兼职官方群 / 讨论区 |
| 帖子卡片 | L1180–1191 | 2 条 community-card：校园墙 ×1、讨论区 ×1 |

### 1.2 问题清单

#### 🔴 致命

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| C1 | 功能缺失 | **3 个官方群 quick-item 均无 `data-screen-target`**，不可点击进入群聊详情或群聊 UI。`campus_rules.md` L29-30 明确要求「官方群结构」「聊天/交友/兼职」，但入口是死胡同。STRUCT-001 已将此列为 P1 优先。 | L1175–1177：3 个 `quick-item` 无任何跳转属性 |
| C2 | 功能缺失 | **2 张 community-card 均无 `data-screen-target`**，不可点击进入帖子详情页。校园墙和讨论区的帖子仅为静态展示，无法查看正文、评论、互动。STRUCT-001 确认「帖子详情页缺失」。 | L1180、L1190：`community-card` 无 `data-screen-target` |
| C3 | 功能缺失 | **社区→发帖→社区的闭环死循环**。publish-hub 中「发帖子」入口的 `data-screen-target` 指向 `community`（L364），而 community 页顶部"＋"按钮指向 `publish-hub`（L1173）。用户无法进入独立的发帖表单页，只能在两个页面间循环。 | L364：`data-screen-target="community"`；L1173：`data-screen-target="publish-hub"` |

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| C4 | 交互缺陷 | **back-btn 初始 hidden**（L1170），如果用户从 publish-hub 或其他页面进入 community，无法通过 back 按钮返回上一页，只能通过底部导航离开。这会破坏用户的浏览连续性。 | L1170：`class="back-btn hidden"` |
| C5 | 视觉问题 | **讨论区（L1179）与 3 个官方群（L1175–1177）在 `quick-grid` 中平级展示**，使用相同的 `quick-item` 样式。讨论区不是群聊，其信息结构与官方群不同，缺乏视觉区分。 | L1174–1179：4 个 `quick-item` 使用完全相同的 DOM 结构和 CSS |
| C6 | 功能缺失 | **无分页/加载更多**。社区仅硬编码 2 条帖子，无"加载更多"触发、无无限滚动、无下拉刷新。真实场景中社区需支持大量内容浏览。 | L1180–1191：仅 2 个静态 `community-card` |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| C7 | 视觉问题 | quick-grid 使用 `overflow-x:auto`（CSS L644），但 4 个 quick-item 刚好填满一行，在窄屏下可能横向滚动。缺少"可滑动"的视觉提示（如渐变遮罩或指示器）。 | CSS L644；HTML L1174–1179 |
| C8 | 数据问题 | 帖子卡片缺少时间戳、作者头像、点赞数和评论数的结构化展示。仅通过 `meta-pill` 标签（「校园墙」「讨论区」）区分来源，信息密度低。 | L1189–1191 |
| C9 | 视觉问题 | community-card 使用与其他卡片相同的 `.community-body` 类名，但样式定义混合在通用的 `.feed-body` / `.order-body` 中，未做差异化（如帖子的多行标题截断、图片预览缩略图）。 | CSS L669–677 |

---

## 二、message（消息）— HTML L1194–1205

### 2.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1195–1197 | page-top：hidden back-btn + 标题 + 空右侧位 |
| 消息列表 | L1198–1204 | 3 条 message-row：校跑腿小吴（订单消息）/ 平台售后助手（售后通知）/ 失物发布者（认领通过） |

### 2.2 问题清单

#### 🔴 致命

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| M1 | 功能缺失 | **3 条 message-row 均无 `data-screen-target`**，消息列表完全不可交互。用户无法从消息列表点击进入聊天详情页。STRUCT-001 已将此列为 P1 优先——「消息→聊天详情贯通缺失」。 | L1199、L1201、L1203：3 个 `message-row` 均无跳转属性 |
| M2 | 功能缺失 | **聊天详情页完全缺失**。`campus_glossary.md` 提及「站内联系」「私信联系」，但原型中不存在对话气泡 UI、消息发送框、输入法适配、键盘弹起处理等聊天核心交互。 | 全文件无 `chat-detail` screen；L1444 处 `screenConfigs` 无此配置项 |

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| M3 | 交互缺陷 | **back-btn 初始 hidden**（L1196），与 community 同问题。用户从首页 header-actions「✉」按钮（L295）进入消息后，无法通过 back 按钮回到首页，只能通过底部导航离开。 | L1196：`class="back-btn hidden"` |
| M4 | 视觉问题 | **缺少未读角标**。message-row 无未读消息数角标、无高亮未读样式。原型中 CSS 已定义 `.profile-dot`（L628–632）可作为角标基础，但 message 页未应用。 | CSS L628；HTML L1198–1204 无角标 |
| M5 | 功能缺失 | **无消息列表操作**。message-row 不支持左滑删除、长按标记已读、置顶会话等消息列表通用交互。 | L1198–1204：仅静态 `message-row` |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| M6 | 视觉问题 | 3 条消息的 `meta-pill` 类型标签（订单消息/售后通知/认领通过）使用不同样式但语义层级不明确——「认领通过」用 `.success` 变体而另外两条用默认蓝底，分类标准不一致（按类型 vs 按状态）。 | L1199–1203 |
| M7 | 数据问题 | 消息时间格式不统一：「19:32」用精确时间、「今天」用相对时间、「今天」第二条又重复。缺少消息列表的统一时间格式化规则（如：刚刚/5分钟前/今天 14:20/昨天/日期）。 | L1199–1203 |
| M8 | 代码问题 | message 页顶部栏右侧位为空的 `<span></span>`（L1197），视觉不对称。community 页在同位置有"＋"功能按钮，message 页留空造成布局不一致。 | L1197 |

---

## 三、profile（我的）— HTML L1207–1348

### 3.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1208–1210 | page-top：hidden back-btn + 标题 |
| 个人头部 | L1211–1224 | profile-card > profile-header：头像😺 + 用户名 + 标签（普通用户/手机号） |
| 钱包区 | L1225–1238 | wallet-card：余额¥0.00 + 充值/提现/账单（纯文本，不可点击） |
| 成为跑腿员 | L1240–1249 | profile-block：图标 + 「成为跑腿员」+ 「立即申请」标签（无交互） |
| 订单状态 | L1251–1272 | profile-block：status-grid 4 项（待接单3→order-list / 配送中2→order-detail / 已送达6→order-list / 已完成18→order-list） |
| 我的交易 | L1274–1294 | profile-block：trade-grid 4 项（我发布的8→publish-hub / 我卖出的3→goods-detail / 我买到的5→goods-detail / 我的收藏12→goods-detail） |
| 常用功能 | L1296–1320 | profile-block：3 个 list-row（地址管理 / 我的优惠券 / 邀请好友） |
| 其他 | L1322–1343 | profile-block：3 个 list-row（系统公告→announcement-detail / 意见反馈 / 关于我们） |
| 退出 | L1345–1348 | logout-btn「⏻ 退出登录」 |

### 3.2 问题清单

#### 🔴 致命

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| P1 | 功能缺失 | **5 个 profile-list-row 无 `data-screen-target`**，STRUCT-001 已识别此问题（缺口清单 1.3）：①地址管理（L1281–1283）、②我的优惠券（L1285–1287）、③邀请好友（L1289–1291）、④意见反馈（L1312–1314）、⑤关于我们（L1316–1318）。这些入口是纯展示，无任何目标页面。 | L1281、L1285、L1289、L1312、L1316 |
| P2 | 功能缺失 | **「成为跑腿员」区块（L1239–1248）无任何交互**。「立即申请」标签为纯装饰 `<span>`，无 `data-screen-target`，无申请流程页面。STRUCT-001 指出「申请流程及审核状态均缺失」。 | L1240–1248 |
| P3 | 功能缺失 | **钱包卡（L1225–1237）不可点击**。余额充值、提现、账单明细仅为 `wallet-meta` 副文本（L1231），无 `data-screen-target`，无钱包详情页。 | L1225–1237 |

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| P4 | 交互缺陷 | **「退出登录」按钮（L1346）无确认弹窗**。STRUCT-001 指出需要 Modal/对话框组件做二次确认。点击即可触发退出，严重破坏用户体验。 | L1346 |
| P5 | 交互缺陷 | **back-btn 初始 hidden**（L1209），与 community/message 同问题。 | L1209 |
| P6 | 功能缺失 | **「我卖出的」「我买到的」「我的收藏」三个交易入口均指向 `goods-detail`**（L1285–1287、L1291），但这些语义指向列表页而非单个商品详情。例如「我卖出的 3 单」应进入卖出列表而非某一商品的详情。 | L1285–1291 |
| P7 | 功能缺失 | **「配送中 2 单」跳转到 `order-detail`**（L1264），但「配送中」可能有多个订单，应进入 order-list 并默认筛选"进行中"，而非单一的订单详情页。与「待接单 3 单→order-list」的跳转逻辑不一致。 | L1264 |
| P8 | 交互缺陷 | **profile-scroll 底部与 footer-nav 可能重叠**。`profile-scroll` 使用 `height: calc(100% - 54px)` 作为内部滚动容器（CSS L414），但底部导航 footer-nav 位于 phone 容器的 `bottom:12px`（CSS L701），两者的安全距离未通过 padding 保证，logout 按钮（L1346）可能被导航遮挡。 | CSS L414；CSS L700–701 |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| P9 | 数据问题 | 用户名「微信用户_fm6p6c」（L1219）暗示微信授权登录，但原型中不存在登录/认证流程。STRUCT-001 已将「登录/认证页」列为缺失。 | L1219；STRUCT-001 §1.1 #3 |
| P10 | 视觉问题 | 头像使用 emoji 😺（L1217），无真实头像上传或默认占位图机制，与小程序实际体验差距大。 | L1217 |
| P11 | 视觉问题 | `profile-header` 的 `::before` / `::after` 伪元素装饰（CSS L340–355）在 `profile-card` 的 `overflow:hidden`（CSS L335）下会被裁剪，装饰效果半不可见。 | CSS L335、CSS L340–355 |

---

## 四、goods-detail（二手详情）— HTML L1087–1103

### 4.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1088–1090 | page-top：back-btn + 标题「二手详情」+ "✉"按钮（→message） |
| 商品卡片 | L1093–1101 | detail-card：banner（market-visual 渐变+「毕业季急出」pill）+ body（标题、描述、价格¥1299+可议价 badge、meta 标签、操作按钮） |

### 4.2 问题清单

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| G1 | 功能缺失 | **「联系卖家」按钮（L1099）跳转到 `message`**，但 message 页消息列表不可交互（见 M1），用户无法真正进入与卖家的聊天。对话链路在 message 层级中断。 | L1099：`data-screen-target="message"`；L1198–1204 无聊天入口 |
| G2 | 功能缺失 | **「我也要发布」按钮（L1100）跳转到 `publish-hub`**，而 publish-hub 中「发二手」入口又跳回 `goods-detail`（L363），形成闭环死循环。用户无法进入真正的二手商品发布表单。 | L1100 → L363：`data-screen-target="goods-detail"` |
| G3 | 功能缺失 | **无商品图片查看器**。`detail-banner` 仅为 CSS 渐变背景装饰（`.market-visual`，CSS L648），无实际商品图片展示。`campus_ui_decisions.md` 提及「二手详情」但未定义图片展示方式。STRUCT-001 将 Image Viewer 列为缺失组件。 | L1093 |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| G4 | 交互缺陷 | **「可议价」badge（L1098）仅为展示标签**，无「出价」或「议价」交互入口。用户看到可议价但不知道如何操作。 | L1098 |
| G5 | 功能缺失 | **无收藏/分享功能**。商品详情页缺少收藏按钮、分享给好友的入口。profile 页有「我的收藏」入口（L1291）但收藏操作本身不可用。 | L1093–1101 |
| G6 | 数据问题 | goods-detail 仅有一个硬编码商品示例。真实场景需要商品 ID 绑定、卖家信息、发布时间、浏览量等数据字段。当前仅「浏览 126」为静态文本。 | L1100 |

---

## 五、lost-detail（失物详情）— HTML L1105–1120

### 5.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1106–1108 | page-top：back-btn + 标题「失物详情」+ 空右侧位 |
| 失物卡片 | L1111–1118 | detail-card：banner（lost-visual 渐变+「平台内认领验证」pill）+ body（标题、描述、meta 标签、操作按钮） |

### 5.2 问题清单

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| L1 | 功能缺失 | **「站内联系」按钮（L1116）跳转到 `message`**，同 G1——消息列表不可点击进入聊天。用户无法从失物详情真正联系发布者。整个「站内联系」承诺无法兑现。 | L1116：`data-screen-target="message"`；L1198–1204 无聊天入口 |
| L2 | 功能缺失 | **「申请认领」→ lost-claim → message 的链路末端断裂**。lost-claim 提交后跳转到 message（L1136），但消息页无聊天详情，认领后的沟通无法进行。 | L1117 → L1136 → message |
| L3 | 功能缺失 | **无失物图片展示**。`detail-banner` 仅为 CSS 渐变（`.lost-visual`，CSS L649），真实场景需要展示失物照片帮助辨认。 | L1111 |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| L4 | 交互缺陷 | **meta-pill「站内联系」（L1117）与操作按钮「站内联系」（L1116）语义重复**。meta-pill 作为元信息标签和操作按钮使用完全相同的文字，用户可能困惑哪个是可点击的。 | L1116–1117 |
| L5 | 数据问题 | 失物信息缺少结构化字段：地点（仅 meta 中有「图书馆三楼」）、时间（仅在描述文本中提到）、失物分类、联系方式偏好。与 `campus_glossary.md` 中「失物招领」定义的信息丰富度有差距。 | L1112–1117 |

---

## 六、lost-claim（认领申请）— HTML L1122–1136

### 6.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1123–1125 | page-top：back-btn + 标题「认领申请」+ 空右侧位 |
| 表单卡片 | L1128–1134 | form-card：3 个字段（物品特征 textarea / 丢失时间 input / 联系方式 input） |
| 提交按钮 | L1136 | primary-btn「提交认领并进入站内联系」→ message |

### 6.2 问题清单

#### 🔴 致命

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| LC1 | 交互缺陷 | **表单提交流程缺失即时反馈**。提交按钮 `data-screen-target="message"`（L1136）直接跳转页面，用户看不到任何"提交成功"或"提交失败"的反馈。STRUCT-001 指出「所有表单提交均直接跳转，无即时反馈」。违反执行智能体自检清单——「是否有入口与出口」「是否存在未解释交互」。 | L1136 |
| LC2 | 功能缺失 | **表单无字段级校验**。三个字段（textarea L1130、input L1132、input L1134）均无 `required` 属性、无 `minlength`、无格式校验。用户可空表单提交。STRUCT-001 将表单校验组件列为 🔴 高优先级缺失组件。 | L1128–1134 |
| LC3 | 功能缺失 | **无特征验证匹配机制**。认领流程的合理设计应为：发布者预设验证问题 → 认领者回答 → 系统匹配判断。当前仅一个自由 textarea，无验证问题、无匹配逻辑、无人工审核入口。与 `campus_glossary.md` 中「认领」定义（"确认某失物为自己的并发起认领请求"）的验证含义脱节。 | L1130 |

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| LC4 | 功能缺失 | **无上传凭证图片**。认领者无法上传证明自己是失主的照片（如购买凭证、物品照片）。仅靠文字描述无法有效验证所有权。 | L1128–1134 |
| LC5 | 交互缺陷 | **「联系方式」字段默认为「站内私信联系」**（L1134），仅暗示站内私信为主要联系方式。但用户可能需要填写手机号等备用联系方式，字段语义模糊，无法引导用户正确填写。 | L1134 |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| LC6 | 视觉问题 | form-card 与 primary-btn 之间无间距控制——表单的最后行直接紧贴提交按钮，缺 `margin-bottom` 或 padding 分隔。 | L1134（form-card 结束）→ L1136（primary-btn） |

---

## 七、announcement-detail（公告详情）— HTML L1350–1360

### 7.1 页面结构

| 区域 | HTML 位置 | 内容 |
|------|----------|------|
| 顶部栏 | L1351–1353 | page-top：back-btn + 标题「公告详情」+ 空右侧位 |
| 公告卡片 | L1357–1359 | detail-card：标题 + 正文 + meta（学校通知/平台公告/今日更新） |

### 7.2 问题清单

#### 🟡 严重

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| A1 | 功能缺失 | **无公告列表/前后切换**。仅一条硬编码公告，用户无法浏览其他公告。从首页 announcement-marquee（L301）或 profile「系统公告」（L1305）进入后，仅看到一条公告，但顶部栏无"上一篇/下一篇"导航。 | L1357–1359 |
| A2 | 功能缺失 | **无分享/转发按钮**。公告详情页没有分享按钮，用户无法将重要学校通知转发给同学。 | L1357–1359 |

#### 🟢 建议

| # | 维度 | 问题描述 | 证据 |
|---|------|---------|------|
| A3 | 视觉问题 | detail-card 缺少公告发布者信息（学校名称/平台/发布人）、发布时间戳（「今日更新」过于模糊）、公告类型图标。信息结构上不如首页 announcement 组件（L300）丰富。 | L1357–1359 |
| A4 | 功能缺失 | **无公告已读状态记录**。用户查看公告后无"已读"标记，无法区分已读/未读公告。首页及 profile 的公告入口无角标提示。 | L1305（profile 入口有 red dot，但没有已读联动逻辑） |

---

## 八、全局分析 — 导航/路由/搜索/CSS/响应式

### 8.1 底部导航（footer-nav）— HTML L1358–1366

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GN1 | 🟡 | 视觉问题 | **导航图标为 CSS 文本字符**（⌂ ✦ ◎ ✉ ◔），不同操作系统/字体下渲染效果差异大，缺少统一图标字体或 SVG 图标。 | L1360–1365 |
| GN2 | 🟡 | 视觉问题 | `nav-item b` 标签使用 `font-weight:500`（CSS L706），非粗体，且图标与文字垂直间距仅 `margin-bottom:5px` —— 在部分设备上可能图标与文字距离过近。 | CSS L706 |
| GN3 | 🟡 | 代码问题 | **底部导航点击直接调用 `showScreen(navTarget)`**（JS L1444），将目标推入 `historyStack`。例如用户在 home→publish→order-detail 的流程中点击底部导航"首页"，historyStack 变为 `['home','publish','order-detail','home']`——按 back 会回退到 order-detail 而非回到上一页。 | JS L1444；JS L1421–1428 |
| GN4 | 🟡 | 交互缺陷 | **导航与可滚动内容的重叠风险**。footer-nav 使用 `position:absolute; bottom:12px` 固定在 phone 容器底部（CSS L700），但 `.content` 的 padding-bottom 为 108px（CSS L186），而 profile-scroll（CSS L414）和 runner-scroll（CSS L261）各自独立滚动，实际滚动区域的底部可能被导航遮挡。 | CSS L186、L700–701 |

### 8.2 Screen 切换机制 — JS L1376–1446

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GS1 | 🟡 | 代码问题 | **`.content` 高度硬编码**为 `calc(920px - 170px)`（CSS L188），`920px` 来自 phone 的 `min-height`（CSS L66）。不同设备实际可视高度差异巨大，overflow:hidden 模式可能导致内容截断。 | CSS L188 |
| GS2 | 🟡 | 代码问题 | **screen 切换动画仅 22ms fadeIn**（CSS L195），缺少页面级过渡动画（如小程序风格的左滑进入/右滑退出）。交互感弱，不符合「高保真原型」的体验目标。 | CSS L195 |
| GS3 | 🟡 | 代码问题 | **hero 区域全局共享，status-row 在所有 screen 中持续显示**（L292）。但 `screenConfigs` 中所有 screen 的 `hero` 属性均为 `true`（JS L1378–1392），没有 screen 级别的 hero 差异化逻辑。非首页 screen 也显示 iOS 状态栏模拟，与小程序实际体验不符。 | JS L1378–1392 |
| GS4 | 🟡 | 交互缺陷 | **hero 的 header-actions 中「✉」(→message) 和「◔」(→profile) 在所有 screen 中显示**（L295–296）。在 profile screen 中显示"个人中心"按钮是多余的；在 message screen 中显示"消息"按钮同样无意义。 | L295–296 |

### 8.3 historyStack 回退逻辑 — JS L1395、L1430–1434

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GH1 | 🟡 | 代码问题 | **底部导航与 historyStack 交互冲突**。底部导航点击 `showScreen(navTarget)` 默认 `pushHistory=true`（JS L1421），导致导航切换被错误地写入回退栈。用户期待的是「导航切换 = 重置栈」，但当前行为是「导航切换 = 追加栈」。 | JS L1444、L1421 |
| GH2 | 🟡 | 代码问题 | **goBack() 无栈底保护**。当 `historyStack` 仅剩 `['home']` 时，按 back 仍会调用 `showScreen('home', false)`，虽不 crash 但无任何反馈。建议在栈底时禁用 back 按钮或以 Toast 提示。 | JS L1430–1434 |
| GH3 | 🟡 | 交互缺陷 | **community / message / profile 的 back-btn 被 hidden**（L1170/L1196/L1209），导致无返回手段。`goBack()` 函数存在但 trigger（back-btn）不可见。 | L1170、L1196、L1209 |

### 8.4 Search Bar 搜索栏 — HTML L298–311

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GS5 | 🔴 | 功能缺失 | **搜索功能完全未实现**。`searchHint` 为 `<span>` 而非 `<input>`（L300），不可输入、不可聚焦、不可提交搜索。整个 search-bar 仅为装饰元素。`campus_ui_decisions.md` 提及「搜索与筛选」但实际原型中搜索不可用。 | L300 |
| GS6 | 🟡 | 交互缺陷 | **filter-button 的选项与当前 screen 上下文不匹配**。filter-menu 的三个选项（跑腿/二手物品/失物招领，L306–308）只适用于 home screen 的视图切换。在 profile / community / message 等 screen 中，`searchHint` 更新为对应 screen 的搜索提示（JS L1390），但 filter-button 仍显示"跑腿"并保持相同的三个下拉选项，语义完全错误。 | L306–308；JS L1389 |

### 8.5 Hero 区域复用 — HTML L289–319

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GH4 | 🟡 | 代码问题 | **hero 的 brand-block `<h1>` 和 `<p>` 文本变更（通过 JS updateHeader L1414–1420）与 screen 的 fadeIn 动画不同步**。`showScreen()` 先切换 screen（L1423），再调用 `updateHeader()`（L1427），两者之间无 Promise/transition 协调。用户可能先看到旧 hero 文本再切换到新文本，造成视觉闪烁。 | JS L1421–1428 |
| GH5 | 🟡 | 视觉问题 | **hero 区域与 page-top 的功能重叠**。非首页 screen 的 page-top 通常包含 `title-stack`（标题+副标题），而 hero 的 brand-block 也展示标题+副标题（JS L1414–1415），形成信息冗余。例如 goods-detail screen 中 page-top 标题为「二手详情」，hero brand 更新为「校园市集」。 | L1089 vs JS L1386（goods-detail 配置） |

### 8.6 CSS 变量体系 — CSS L8–19

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GC1 | 🟢 | 代码问题 | **缺少字号/字重/间距变量**。当前仅定义了颜色和圆角变量。`font-size`、`font-weight`、`padding`、`margin`、`gap` 等高频值分散在 CSS 各处硬编码（如 L116 `font-size:12px`、L156 `font-size:28px`、L644 `gap:10px`），不利于后续主题切换或设计系统落地。 | CSS L8–19 |
| GC2 | 🟢 | 代码问题 | **缺少深色模式变量**。`@media (prefers-color-scheme: dark)` 覆盖未定义，全部颜色为硬编码亮色主题值。 | 全 CSS |
| GC3 | 🟢 | 代码问题 | `--line`（L17）和 `--shadow`（L18）的 alpha 值（0.08/0.10）极低，在低对比度显示器上几乎不可见。Shadow 和分隔线可能在某些环境下"消失"。 | CSS L17–18 |

### 8.7 响应式处理 — CSS L707–718

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GR1 | 🟡 | 代码问题 | **仅一个 `@media (max-width: 420px)` 断点**。无平板（≥768px）适配、无横屏适配、无折叠屏适配。`min-height:920px` 的 phone 容器在真实小屏设备（如 iPhone SE 320px 宽、竖屏 568px 高）上会严重溢出。 | CSS L707 |
| GR2 | 🟡 | 交互缺陷 | **`.content` 固定高度 + `phone` 的 `min-height: 920px`**（CSS L188+L66），在真实移动端竖屏（常见高度 667–896px）中，phone 容器超出视口需要外部滚动，但 `.app-shell` 的 `min-height:100vh`（CSS L59）又限定最小高度，可能在短屏设备上产生尴尬的嵌套滚动。 | CSS L59、L66、L188 |
| GR3 | 🟢 | 代码问题 | **420px 断点中 `.card-grid.two-col` 改为单列**（CSS L714），但跑腿卡片设计初衷为双列展示（4 张卡片 2×2），单列后卡片过长，比例失调。 | CSS L714 |
| GR4 | 🟢 | 代码问题 | **`.split`（表单双列布局）在 420px 下改为单列**（CSS L714），但双列表单（如「取货地点/送达地点」L345–346）改为单列后会失去并排提示的语义关联，需要重新设计标签层级。 | CSS L714；L345–346 |

### 8.8 全局组件化问题

| # | 严重度 | 维度 | 问题描述 | 证据 |
|---|:------:|------|---------|------|
| GC4 | 🟡 | 代码问题 | **`.page-top` 在 13 个 screen 中重复硬编码**（除 home 外的所有 screen）。STRUCT-001 §3.1 已识别此重复结构。back-btn 的 hidden 状态、右侧按钮的存在与否、title-stack 的内容——这些在 13 处各自独立定义，修改一致行为需改 13 处。 | L331, L360, L370, L790, L802, L858, L924, L1044, L1072, L1089, L1107, L1124, L1169, L1195, L1209, L1352 |
| GC5 | 🟡 | 代码问题 | **`data-screen-target` 有 2 种绑定方式但语义不同**：① `document.querySelectorAll('[data-screen-target]')` 绑定 click→showScreen（JS L1443）；② `data-nav-target` 绑定 click→showScreen（JS L1444）。两者调用同一函数但语义不同——前者是"内部跳转"，后者是"导航切换"。但 JS 中未区分处理，导致 historyStack 行为一致（均 push），造成 GN3 问题。 | JS L1443–1444 |

---

## 九、与 STRUCT-001 交叉验证

| STRUCT-001 发现 | EXEC-002 验证结果 | 交叉引用 |
|-----------------|-----------------|---------|
| community 无二级交互入口 | ✅ 确认：3 个群入口 + 2 个帖子卡片均无 data-screen-target | C1, C2 |
| message 消息列表不可点击 | ✅ 确认：3 条消息均无 data-screen-target，聊天详情页完全缺失 | M1, M2 |
| profile 5 个入口无目标页面 | ✅ 确认：地址管理/优惠券/邀请好友/意见反馈/关于我们均无跳转 | P1 |
| 「成为跑腿员」无申请流程 | ✅ 确认：无 data-screen-target，无申请页，无审核态 | P2 |
| 「发帖子」跳转 community 而非发帖页 | ✅ 确认：community ⇄ publish-hub 形成闭环死循环 | C3 |
| goods-detail / lost-detail 「联系卖家/站内联系」跳到 message 但 message 不可交互 | ✅ 确认：联系链路在 message 层彻底中断 | G1, L1, L2 |
| 表单提交无即时反馈 | ✅ 确认：lost-claim 提交直接跳 message | LC1 |
| Toast/Modal/Skeleton/表单校验等关键组件缺失 | ✅ 确认：原型中均无对应实现 | LC2, P4 |
| 搜索功能不可用 | ✅ 确认：search-bar 为纯装饰 span 而非 input | GS5 |
| back-btn hidden 问题 | ✅ 确认：community/message/profile 三屏 back-btn 均 hidden | C4, M3, P5 |

---

## 十、执行智能体自检清单

| # | 检查项 | 状态 |
|---|-------|:----:|
| 1 | 页面是否完整（7 个 screen + 全局分析均已独立成节） | ✅ |
| 2 | 状态是否齐全（各 screen 的状态覆盖已在各节问题中讨论） | ✅ |
| 3 | 是否有入口与出口（已检查所有 data-screen-target 链路） | ⚠️ 多处链路断裂 |
| 4 | 是否使用统一组件（已识别 .page-top 等 13 次重复硬编码） | ❌ 未统一 |
| 5 | 是否使用统一命名（data-screen-target / data-nav-target 语义混用） | ⚠️ 部分混用 |
| 6 | 是否存在未解释交互（公告已读、退出确认、消息角标等） | ❌ 多处未解释 |
| 7 | 是否满足可开发交付要求 | ❌ 需补完 6 项致命问题后方可 |

### 致命问题汇总（需阻断性修复）

| 编号 | 位置 | 问题 |
|------|------|------|
| 🔴 C1 | community | 3 个官方群入口不可交互 |
| 🔴 C2 | community | 2 个帖子卡片不可点击 |
| 🔴 C3 | community | 发帖链路形成 publish-hub ⇄ community 死循环 |
| 🔴 M1 | message | 消息列表完全不可交互 |
| 🔴 M2 | message | 聊天详情页完全缺失 |
| 🔴 P1 | profile | 5 个功能入口无目标页面 |
| 🔴 P2 | profile | 成为跑腿员申请流程缺失 |
| 🔴 P3 | profile | 钱包卡不可点击 |
| 🔴 LC1 | lost-claim | 表单提交无反馈 |
| 🔴 LC2 | lost-claim | 表单无字段校验 |
| 🔴 LC3 | lost-claim | 无特征验证匹配机制 |
| 🔴 GS5 | 全局 | 搜索功能完全未实现 |

---

## 十一、建议修复优先级

| 优先级 | 行动 | 关联致命问题 |
|:------:|------|------------|
| 🔴 P0 | **实现聊天详情页**，为所有 message-row 添加 data-screen-target | M1, M2, G1, L1, L2 |
| 🔴 P0 | **为 community 添加二级交互**：群入口 → 群聊详情、帖子卡片 → 帖子详情、独立发帖表单页 | C1, C2, C3 |
| 🔴 P0 | **实现搜索输入功能**：searchHint span → input，增加搜索提交逻辑 | GS5 |
| 🟡 P1 | **补全 profile 5 个目标页面**（地址管理/优惠券/邀请好友/意见反馈/关于我们）+ 钱包详情 + 跑腿员申请页 | P1, P2, P3 |
| 🟡 P1 | **为 lost-claim 添加表单校验 + 提交反馈**（Toast/结果态），并设计特征验证问题机制 | LC1, LC2, LC3 |
| 🟡 P1 | **修复底部导航 historyStack 行为**：导航切换时清空或重置返回栈 | GN3, GH1 |
| 🟡 P1 | **动态控制 back-btn 显隐**：非主页 screen 中 back-btn 默认 visible，主页 screen（home/community/message/profile 作为底导入口时）hidden | C4, M3, P5, GH3 |
| 🟢 P2 | **统一 .page-top 为模板/组件**，消除 13 处重复硬编码 | GC4 |
| 🟢 P2 | **建立字号/间距/字重 CSS 变量体系**，补充深色模式支持 | GC1, GC2 |
| 🟢 P2 | **为 goods-detail/lost-detail 添加真实图片展示**，补充发布表单页 | G3, L3, G2 |
